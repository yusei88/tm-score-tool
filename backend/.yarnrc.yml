nodeLinker: "node-modules"
# Use node-modules linker for compatibility with tools expecting node_modules layout

# Set a numeric HTTP timeout (milliseconds) to satisfy schema expectations
httpTimeout: "60000"
